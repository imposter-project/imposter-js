plugin: openapi
specFile: order-api.yaml

resources:
  - path: /orders
    method: post
    steps:
      - type: script
        file: order-api.js
    response:
      statusCode: 201
      content: "${stores.request.responseBody}"
      template: true
